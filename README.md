# Bilal Aziz — In the making

Minimal black-and-white portfolio placeholder. Plain HTML, CSS and JavaScript; no build step.

Open `index.html` locally to preview. Enable GitHub Pages from `main` and `/ (root)` in repository Settings → Pages.
